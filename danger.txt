Критические — исправить немедленно
CRITICAL
AdminPanel.jsx
RLS не включён на таблицах — любой может читать/удалять ВСЕ данные
В миграциях нет ни одной строки ALTER TABLE … ENABLE ROW LEVEL SECURITY. Это означает, что любой человек, у кого есть Supabase Anon Key (а он встроен в JS-бандл и виден в DevTools), может через Supabase REST API прочитать всю таблицу drivers (ФИО, email, telegram_id всех водителей), всю weekly_incomes (финансовые данные), и выполнить массовое удаление. Политики INSERT/DELETE для drivers созданы, но RLS не активирован — политики просто игнорируются без него.
Исправление: в Supabase Dashboard → Table Editor → каждая таблица → Enable RLS. Затем написать строгие политики: SELECT только для owner (по telegram_id), admin — через проверку is_admin.
CRITICAL
App.jsx:18
Авторизация через initDataUnsafe — подделывается в браузере
Приложение использует tg.initDataUnsafe.user.id для определения личности пользователя. Это поле не верифицировано — любой разработчик может открыть DevTools, переопределить window.Telegram.WebApp.initDataUnsafe.user.id нужным значением и войти как любой водитель или администратор. Для верификации нужен серверный HMAC-SHA256 от tg.initData (строки, а не объекта).
Исправление: на backend (n8n или Edge Function) проверять initData строку через HMAC-SHA256 с Bot Token как секретом, согласно документации Telegram. Фронтенд не должен доверять initDataUnsafe без этой проверки.
🟠 Высокий приоритет
HIGH
AdminPanel.jsx
Права администратора проверяются только на клиенте
Кнопка «Админ» скрыта если user.is_admin === false, но AdminPanel делает прямые Supabase-запросы (.from('drivers').delete(), .insert(), bulk income rollback) без проверки прав на стороне БД. Кто угодно может вызвать эти же методы через консоль браузера.
Исправление: RLS-политики с проверкой is_admin, либо вынести опасные операции (удаление, импорт) в Supabase Edge Functions с серверной проверкой роли.
HIGH
VerificationScreen.jsx:43
URL n8n webhook захардкожен и открыт в публичном репозитории
URL https://primary-production-e36b.up.railway.app/webhook/send-code виден в исходном коде публично. Злоумышленник может бесплатно спамить этот эндпоинт тысячами запросов, расходуя ресурсы Railway и отправляя письма от имени вашего сервиса (email-спам через вашу инфраструктуру).
Исправление: добавить секрет (shared token) в заголовок запроса, хранить URL в VITE_N8N_WEBHOOK_URL, добавить rate-limit в n8n (не более 3 запросов с одного email за 10 минут).
HIGH
xlsx@0.18.5
Уязвимая версия библиотеки xlsx (SheetJS)
Версия 0.18.5 содержит известные CVE (прототипное загрязнение, XXE-инъекции при парсинге злонамеренных файлов). Если администратор загрузит подброшенный Excel-файл, это может привести к XSS или краже данных из браузера.
Исправление: обновить до актуальной версии, или перейти на exceljs. Перед парсингом проверять MIME-тип файла на сервере.
🔵 Средний приоритет
MED
index.html
Отсутствуют HTTP Security Headers
Нет CSP, X-Frame-Options, Referrer-Policy, Permissions-Policy — как выявлено в предыдущем аудите. Особенно важно для Telegram Mini App.
Исправление: добавить в vercel.json блок headers.
MED
VerificationScreen.jsx
Нет rate-limit на запрос кода верификации
Функция request_verification_code вызывается без ограничений. Любой может перебирать email-адреса для разведки (узнавать, зарегистрирован ли email), или заваливать конкретного водителя кодами верификации.
Исправление: в PostgreSQL-функции добавить проверку времени последней отправки (cooldown 60 секунд).
MED
GitHub
Репозиторий публичный — видна вся архитектура
Публичный репозиторий раскрывает структуру БД, имена таблиц, логику авторизации, URL Railway, схему RPC-функций. Это облегчает целевые атаки.
Исправление: перевести репозиторий в приватный режим, если нет причин держать его публичным.
✅ Сделано правильно
GOOD
Supabase credentials через env-переменные
URL и Anon Key берутся из import.meta.env, не захардкожены в коде. Правильно.
GOOD
Верификационные коды — через SECURITY DEFINER функции
Таблица verification_codes не доступна напрямую с клиента; генерация и проверка кода идут через серверные Postgres-функции. Хорошая архитектура.
GOOD
Одноразовые коды с TTL 10 минут и удалением после использования
Коды удаляются сразу после успешной верификации и истекают через 10 минут. Правильно.
GOOD
is_active проверяется на уровне DB-функции
Функция request_verification_code проверяет is_active в БД до отправки кода. Деактивированный водитель не сможет войти.

Вот детальный разбор после анализа реального кода. Самое важное:
Две критические проблемы, которые нужно закрыть прямо сейчас:
1. RLS не включён. Ни в одной миграции нет ALTER TABLE … ENABLE ROW LEVEL SECURITY. Политики INSERT/DELETE для drivers созданы, но они мертвы без активации RLS. Любой человек с Anon Key (он виден в DevTools у любого пользователя) может сделать:
jsfetch('https://ваш-проект.supabase.co/rest/v1/drivers?select=*', {
  headers: { 'apikey': 'ANON_KEY' }
})
// → получит все ФИО, email, telegram_id всех водителей
Идите в Supabase Dashboard → Table Editor → каждая таблица → включите RLS.
2. initDataUnsafe не верифицирован. Любой может открыть DevTools и написать window.Telegram.WebApp.initDataUnsafe.user.id = 123456789 и войти под любым водителем. Верификация initData должна быть на сервере через HMAC-SHA256 с Bot Token.
Архитектурно проект сделан очень грамотно — SECURITY DEFINER функции для верификационных кодов, TTL, env-переменные. Осталось закрыть несколько важных дыр в самой Supabase. 